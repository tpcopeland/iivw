% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iivw_weight.R
\name{iivw_weight}
\alias{iivw_weight}
\title{Compute Inverse Intensity and Visit Weights}
\usage{
iivw_weight(
  data,
  id,
  time,
  visit_cov,
  treat = NULL,
  treat_cov = NULL,
  wtype = NULL,
  stabcov = NULL,
  lagvars = NULL,
  entry = NULL,
  truncate = NULL,
  prefix = "iivw_"
)
}
\arguments{
\item{data}{A data.frame in long panel format (one row per subject-visit).}

\item{id}{Character string; column name for subject identifier.}

\item{time}{Character string; column name for continuous visit time.}

\item{visit_cov}{Character vector; covariate column names for the visit
intensity Cox model.}

\item{treat}{Character string or NULL; column name for binary (0/1)
time-invariant treatment indicator. If specified, IPTW weights are
computed and the default weight type becomes FIPTIW.}

\item{treat_cov}{Character vector or NULL; covariates for the propensity
score model. Defaults to \code{visit_cov} if NULL and \code{treat} is specified.}

\item{wtype}{Character or NULL; weight type override. One of \code{"iivw"},
\code{"iptw"}, or \code{"fiptiw"}. If NULL (default), auto-detected: FIPTIW when
\code{treat} is specified, IIW otherwise.}

\item{stabcov}{Character vector or NULL; covariates for the stabilized IIW
numerator model. When specified, stabilized weights are computed as
\code{exp(xb_stab - xb_full)} instead of \code{exp(-xb_full)}.}

\item{lagvars}{Character vector or NULL; time-varying covariates to lag by
one visit within each subject. Creates columns named \verb{\{var\}_lag1}.}

\item{entry}{Character string or NULL; column name for subject-specific
study entry time. Default start time is 0 for all subjects.}

\item{truncate}{Numeric vector of length 2 or NULL; percentile bounds
(0-100) for weight truncation (winsorization). E.g., \code{c(1, 99)}.}

\item{prefix}{Character string; prefix for generated weight column names.
Default \code{"iivw_"}.}
}
\value{
A data.frame of class \code{"iivw_weights"} with added weight columns:
\itemize{
\item \verb{\{prefix\}iw}: IIW component (if weight type includes IIW)
\item \verb{\{prefix\}tw}: IPTW component (if weight type includes IPTW)
\item \verb{\{prefix\}weight}: Final combined weight
}

The returned object has attributes storing metadata: \code{iivw_id}, \code{iivw_time},
\code{iivw_weight_type}, \code{iivw_weight_var}, \code{iivw_prefix}, \code{iivw_treat},
\code{iivw_diagnostics}.
}
\description{
Computes inverse intensity of visit weights (IIW) to correct for informative
visit processes in longitudinal clinic-based data. Optionally computes IPTW
for confounding by indication and their product (FIPTIW).
}
\details{
Visit intensity is modeled via an Andersen-Gill recurrent-event Cox model on
the counting process of visits. Weights are the inverse of the estimated
conditional intensity ratio (Buzkova & Lumley 2007).
}
\examples{
# Simulate panel data
set.seed(42)
n_subj <- 50
n_vis <- 4
d <- data.frame(
  id = rep(1:n_subj, each = n_vis),
  time = rep((0:(n_vis - 1)) * 6, n_subj),
  severity = rnorm(n_subj * n_vis, 3, 1)
)

# IIW weights
result <- iivw_weight(d, id = "id", time = "time", visit_cov = "severity")
}
\references{
Buzkova P, Lumley T (2007). "Longitudinal data analysis for generalized
linear models with follow-up dependent on outcome-related variables."
\emph{Canadian Journal of Statistics}, 35(4), 485-500.

Tompkins JD, Dubin JA, Wallace MP (2025). "Fully inverse probability of
treatment and intensity weighted estimators for non-randomized longitudinal
studies subject to irregular visit processes."
}
